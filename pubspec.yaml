name: app
description: "A new Flutter project."
publish_to: 'none'

version: 0.7.2+3

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # PlombiPro Dependencies
  firebase_core: ^3.4.0
  supabase_flutter: ^2.5.3

  # State Management
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5

  # Biometric Authentication
  local_auth: ^2.3.0
  flutter_secure_storage: ^9.2.2

  image_picker: ^1.1.2
  permission_handler: ^11.3.1
  pdf: ^3.11.0
  intl: any
  dio: ^5.5.0+1
  http: ^1.2.1
  freezed_annotation: ^2.4.1
  google_fonts: ^6.2.1
  flutter_dotenv: ^6.0.0
  go_router: ^14.6.1
  url_launcher: ^6.3.0
  path_provider: ^2.1.3
  open_filex: ^4.4.0
  signature: ^5.5.0
  flutter_stripe: ^11.5.0

  cupertino_icons: ^1.0.8
  fl_chart: ^0.69.0
  uuid: ^4.4.0
  json_annotation: ^4.9.0
  sentry_flutter: ^8.14.2
  excel: ^4.0.6
  csv: ^6.0.0
  introduction_screen: ^4.0.0
  shared_preferences: ^2.2.3

  # Modernization 2025 - Animations & UI
  animate_do: ^3.3.4
  shimmer: ^3.0.0
  lottie: ^3.1.2
  tutorial_coach_mark: ^1.2.11
  cached_network_image: ^3.3.1

  # Offline storage (Phase 10)
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  connectivity_plus: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # PlombiPro Dev Dependencies
  build_runner: ^2.4.9
  freezed: ^2.4.7
  riverpod_generator: ^2.4.0
  riverpod_lint: ^2.3.10

  flutter_lints: ^4.0.0
  json_serializable: ^6.8.0

dependency_overrides:
  app_links: 6.4.1

flutter:
  uses-material-design: true
  assets:
    - assets/templates/